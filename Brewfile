# Homebrew dependencies for this repo. Install with: brew bundle
# Python 3.12 is the pinned interpreter for this repo's tooling.
brew "python@3.12"
brew "ffmpeg"  # provides ffprobe for interlace and stream format detection
brew "mpv"  # media player for channel stream playback
